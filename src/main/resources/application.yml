server:
  port: 8888

spring:
  application:
    name: zgy-code-generator
  thymeleaf:
    cache: false
    encoding: UTF-8
    suffix: .html
    prefix: classpath:/templates/
  web:
    resources:
      static-locations: classpath:/static/

# generator default config
generator:
  author: zgy
  # outputPath: d://s11/hello
  outputPath: /Users/zgy/mycode/myprojects/generator
  package:
    module: hello
    parent: com.zgy.learn
  database:
    user: root
    password: 123456
    url: localhost:3308/test
    tables: user,userinfo
